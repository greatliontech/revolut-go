# Never commit secrets or local env files.
*.pem
*.key
*.cer
*.crt
*.p12
*.pfx
.env
.env.*
!.env.example

# Tool binaries accidentally built in the repo root.
/auth-bootstrap
/revogen

# Scratch output from `task test:gen-all`.
/.revogen-scratch
/ob-jwks
